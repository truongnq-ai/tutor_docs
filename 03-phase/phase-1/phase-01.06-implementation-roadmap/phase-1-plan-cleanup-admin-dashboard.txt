Tôi đã đọc kỹ **báo cáo hiện trạng admin dashboard** bạn cung cấp .
Dưới đây là **nhận xét ngắn gọn, đi thẳng vào bản chất**, sau đó là **kế hoạch cleanup ở mức chiến lược** (đúng mong muốn của bạn), **chưa viết prompt, chưa đi vào thao tác cụ thể**.

---

## I. Nhận xét ngắn gọn về hiện trạng Admin Dashboard

### 1. Tổng quan một câu

> **Admin dashboard hiện tại là một “full-featured admin system” vượt xa nhu cầu Phase 1, nhưng có giá trị lớn về mặt tài sản kỹ thuật nếu được vô hiệu hóa đúng cách.**

Nó **không bừa**, không “code rác”, mà là:

* kiến trúc tốt
* stack hiện đại
* domain separation rõ
* nhưng **scope quá rộng so với Phase 1**

---

### 2. Những điểm TỐT và nên giữ làm nền

* **Tech stack rất mạnh & mới**

  * Next.js App Router
  * React 19
  * TypeScript strict
  * Tailwind, RHF + Zod, Axios client chuẩn
* **Cấu trúc module theo domain** (chapters / skills / exercises / users)
* **Auth flow sạch**

  * httpOnly cookie
  * middleware guard
  * token refresh queue (hiếm khi code amateur làm đúng)
* **Form & validation tốt**, đặc biệt ở exercise
* **API client & service layer rõ ràng**

👉 Kết luận: **KHÔNG phải code “ném đi”**, mà là **code “đang hoạt động quá mức”**.

---

### 3. Những điểm VƯỢT SCOPE PHASE 1 (nhưng chưa kết luận xóa)

Chỉ liệt kê bản chất, **chưa bàn xử lý**:

* **Trial user & trial service**
* **AI quality / prompt template / generate / review**
* **System pages (metrics / logs / health)**
* **Device management**
* **Dashboard metrics phức tạp (charts, stats, calendar, map)**
* **Exercise review workflow đầy đủ**
* **React Query đã setup nhưng không dùng → dư thừa kiến trúc**

👉 Tất cả các phần này **không phục vụ Phase 1 core**, nhưng **có thể là tài sản tái sử dụng**.

---

### 4. Điểm nguy hiểm nhất (cần xử lý cẩn trọng)

Không phải feature, mà là **“tín hiệu sai cho dev & cho chính bạn trong 2–3 tuần nữa”**:

* Menu đang **mời gọi rất nhiều thứ không được dùng**
* Route vẫn truy cập được dù không cần
* Có logic frontend **tự suy luận nghiệp vụ**
* Có service gọi backend **ngoài Phase 1 intent**

👉 Nếu không cleanup, **frontend sẽ kéo backend ra khỏi Phase 1**.

---

## II. Kế hoạch cleanup admin dashboard (mức chiến lược, chưa chi tiết)

Kế hoạch này **đúng với mong muốn của bạn**:

* **tận dụng code**
* **không xóa sạch**
* **disable – không phá – có thể mở lại sau**

Tôi chia làm **4 lớp cleanup**, theo thứ tự bắt buộc.

---

## III. KẾ HOẠCH CLEANUP – ADMIN DASHBOARD

### LỚP 1 — Cleanup “QUYỀN TRUY CẬP” (Access Layer)

*(Làm trước tiên, tác động lớn nhất, ít rủi ro)*

**Mục tiêu**

> Những gì Phase 1 không dùng → *không thể truy cập từ UI và URL*, dù code còn đó.

**Hành động ở mức chiến lược**

* Xác định **danh sách route PHASE 1 ĐƯỢC PHÉP**
* Toàn bộ route khác:

  * loại khỏi menu
  * chặn ở middleware hoặc layout level
* Không xóa file page, **chỉ vô hiệu hóa routing**

➡️ Sau bước này:

* User không “đi lạc”
* Dev không test nhầm
* Backend không bị gọi sai

---

### LỚP 2 — Cleanup “MENU & UX SIGNAL”

*(Rất quan trọng về mặt tâm lý & định hướng)*

**Mục tiêu**

> Menu phải nói đúng: “Phase 1 chỉ có thế này”.

**Hành động ở mức chiến lược**

* Giữ menu **tối thiểu**, ví dụ:

  * Dashboard (rút gọn)
  * Users (parent / student / admin nếu cần)
  * Content (chapter / skill / exercise ở mức quản trị nội dung)
* Loại khỏi menu:

  * Trial
  * AI Quality
  * System
  * Prompt template
  * Device
* Không comment trong JSX → **tách config menu theo Phase**

➡️ Sau bước này:

* Không ai bị “dụ” dùng feature ngoài scope
* UX phản ánh đúng Phase 1 intent

---

### LỚP 3 — Cleanup “LOGIC THAM GIA NGHIỆP VỤ”

*(Cẩn thận, cần hiểu rõ code trước)*

**Mục tiêu**

> Frontend **KHÔNG được suy luận luật học tập**.

**Những nhóm logic cần xem xét vô hiệu hóa / thu hẹp**

* Review workflow phức tạp (approve/reject/needs_revision)
* Stats tự tính ở frontend
* Logic generate / AI metadata
* Logic liên quan trial / status nâng cao

**Nguyên tắc**

* Phase 1 admin **chỉ quản lý nội dung & gán**, không “đánh giá học tập”
* Logic nào không phục vụ CRUD / assign → đưa về trạng thái *disabled asset*

---

### LỚP 4 — Cleanup “TÀI SẢN KỸ THUẬT DƯ THỪA”

*(Làm sau cùng, khi đã khóa hành vi)*

**Mục tiêu**

> Code còn đó, nhưng không gây nhiễu.

Bao gồm:

* Folder legacy naming (`ecommerce/`)
* Component không dùng
* Duplicate util file
* React Query setup nhưng chưa dùng

👉 Lớp này **không ảnh hưởng nghiệp vụ**, nên làm cuối.

---

## IV. Thứ tự thực hiện cleanup (đề xuất)

1. **Access layer (route + middleware)**
2. **Menu / UX signal**
3. **Logic nghiệp vụ không thuộc Phase 1**
4. **Dọn tài sản kỹ thuật dư thừa**

➡️ Làm đúng thứ tự này sẽ:

* không phá backend
* không mất code
* không tạo nợ kỹ thuật mới

---

## V. Kết luận ngắn

* Admin dashboard **không phải vấn đề**, mà là **đang “quá tốt” so với Phase 1**
* Ý tưởng của bạn *giữ code nhưng disable* là **đúng và khôn ngoan**
* Cleanup phải đi từ **quyền truy cập → UX → logic → code**
* Chưa nên viết prompt cleanup ngay, vì:

  * cần **chốt ranh giới Phase 1 admin dùng gì**

---
